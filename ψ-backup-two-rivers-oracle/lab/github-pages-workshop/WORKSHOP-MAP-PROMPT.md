# Workshop: สร้างแผนที่จาก GPS Data

## สำหรับนักศึกษา — ให้ Oracle ของคุณสร้างแผนที่

### Step 1: ดาวน์โหลดไฟล์ CSV

ไฟล์ GPS data ของการเดินทาง เชียงใหม่ → พิษณุโลก:

```
https://raw.githubusercontent.com/Soul-Brews-Studio/two-rivers-oracle/main/ψ/lab/github-pages-workshop/travel-phitsanulok.csv
```

### Step 2: Copy prompt นี้ให้ Oracle ของคุณ

```
สร้าง HTML แผนที่จากไฟล์ CSV นี้ให้หน่อย

ข้อมูล CSV มี columns: device, model, battery, charging, lat, lon, accuracy, source, place, place_type, locality, address, updated

ต้องการ:
1. ใช้ Leaflet.js (CDN) สร้างแผนที่
2. วาง marker จาก lat, lon ในไฟล์ CSV
3. ลากเส้นเชื่อมจุดตามลำดับเวลา
4. คลิก marker แล้วแสดง: สถานที่, เวลา, แบตเตอรี่
5. Dark theme (พื้นหลังเข้ม)
6. ใส่ชื่อ Oracle ของเราใน footer

CSV data (328 rows):
[วาง CSV content ที่นี่ หรือให้ Oracle อ่านไฟล์]
```

### Step 3: อัปโหลดเป็น GitHub Pages

1. Save เป็น `index.html`
2. อัปโหลดไปที่ repo `username.github.io`
3. เปิด `https://username.github.io`

---

### ตัวอย่าง — ดูก่อนทำ

- **2D Map**: `travel-map.html` — Leaflet.js + timeline
- **3D Train**: `travel-3d.html` — Three.js + animated train

ทั้งสองไฟล์ใช้ข้อมูลจาก `travel-phitsanulok.csv` เดียวกัน

---

### Tips

- ไม่จำเป็นต้องใช้ทุก 328 rows — ให้ Oracle เลือก waypoints หลักๆ ก็ได้
- ลองเพิ่ม: popup รูปภาพ, animation, สี custom, ข้อมูลแบตเตอรี่
- ใครอยากทำ 3D ก็ใช้ Three.js ได้เลย

---

🤖 สร้างโดย Two Rivers (สองแคว) — Teaching Oracle
